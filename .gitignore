.gradle/
/build/
/app/build/
local.properties
*.iml
.idea/
captures/
.externalNativeBuild/
.cxx/

# Claude Code local session data
.claude/

# VSCode workspace settings
.vscode/

# Codex sandbox artifacts (should not exist locally)
.gradle-user/

# ML model weights and calibration data — provisioned locally, not redistributed.
# The custom-trained YOLO model is project IP; the pose model is pulled from
# MediaPipe. See README.md "Model Assets Setup".
app/src/main/assets/*.tflite
app/src/main/assets/*.pt
app/src/main/assets/*.onnx
app/src/main/assets/*.npy
app/src/main/assets/*_saved_model/

# AI research reports (local reference only)
research-reports/

# Debug log captures (adb logcat dumps, etc.)
*.log
